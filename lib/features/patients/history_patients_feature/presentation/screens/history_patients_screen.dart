import 'package:flutter/material.dart';

class HistoryPatientsScreen extends StatelessWidget {
  const HistoryPatientsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('سجل المريض'),
      ),
    );
  }
}
